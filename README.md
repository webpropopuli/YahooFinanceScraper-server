# YahooFinanceScraper-server
The back-end to the React client.

## Deploy

add local Heroku
`heroku login`
`heroku create` with existing git
`heroku ps:scale web=1`
`heroku open`
`heroku local web` runs on PORT5000
